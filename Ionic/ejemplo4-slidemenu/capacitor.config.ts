import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ejemplo4-slidemenu',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;

import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ejemplo1-hello-tabs',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;

import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ejemplo6-login-firebase',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;

import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ejemplo7-login-facebook',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;

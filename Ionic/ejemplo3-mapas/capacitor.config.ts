import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ejemplo3-mapas',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
